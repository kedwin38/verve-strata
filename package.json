{
  "name": "verve-strata",
  "version": "1.0.0",
  "private": true,
  "description": "Verve Strata — Dependency Provenance Intelligence. Supply-chain stratigraphy for engineering teams: when each dependency entered your codebase, who introduced it, what it has cost you since, and what the next merge would add.",
  "license": "MIT",
  "workspaces": [
    "server",
    "web"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "npm run build --workspace server && npm run build --workspace web",
    "test": "npm run test --workspace server",
    "typecheck": "npm run typecheck --workspace server && npm run typecheck --workspace web",
    "dev:server": "npm run dev --workspace server",
    "dev:web": "npm run dev --workspace web",
    "start": "node server/dist/index.js"
  }
}
